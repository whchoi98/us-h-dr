apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: onprem-eks
  region: us-west-2
  version: "1.33"
vpc:
  id: "${ONPREM_VPC_ID}"
  subnets:
    private:
      us-west-2a:
        id: "${ONPREM_PRIVATE_SUBNET_AZA}"
      us-west-2b:
        id: "${ONPREM_PRIVATE_SUBNET_AZB}"
managedNodeGroups:
  - name: ng-main
    instanceType: t4g.2xlarge
    desiredCapacity: 8
    minSize: 8
    maxSize: 8
    volumeSize: 100
    volumeType: gp3
    amiFamily: AmazonLinux2023
    iam:
      instanceRoleARN: "${ONPREM_EKS_NODE_ROLE_ARN}"
cloudWatch:
  clusterLogging:
    enableTypes:
      - api
      - audit
      - authenticator
      - controllerManager
      - scheduler
secretsEncryption:
  keyARN: "${ONPREM_EKS_SECRETS_KEY_ARN}"
addons:
  - name: vpc-cni
  - name: coredns
  - name: kube-proxy
  - name: aws-ebs-csi-driver
  - name: aws-efs-csi-driver
  - name: eks-pod-identity-agent
  - name: amazon-cloudwatch-observability
  - name: aws-load-balancer-controller
    version: v3.1.0
  - name: karpenter
    version: v1.9.0
